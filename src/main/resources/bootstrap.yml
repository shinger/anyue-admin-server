server:
  address: 127.0.0.1
  port: 8003
spring:
  application:
    name: anread-server-admin
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        ip: 127.0.0.1
      config:
        server-addr: 127.0.0.1:8848
  config:
    import: optional:nacos:anread-server-admin.yml?group=DEFAULT_GROUP

  data:
    mongodb:
      host: 127.0.0.1
      port: 27017
      database: anread_books
      username: root
      password: '123456'
      authentication-database: admin

#mybatis-plus:
#  mapper-locations: classpath:mapper/*.xml
